1. Login to cpanel and go to Setup python app
2. Click edit and copy path
3. Go to terminal and paste the path
4. Enter and type python 
5. Paste the following code line by line:
from django.conf import settings
settings.configure(PASSWORD_HASHERS=['django.contrib.auth.hashers.PBKDF2PasswordHasher'])

import django
django.setup()

from django.contrib.auth.hashers import make_password
print(make_password("Password@123!"))

5. you will get output
6. Next go to phpMyAdmin and open database, go to auth_user table and edit password to the output
7. Try to login to admin panel wit /admin/ url to site, if not logged in you have made a mistake.